<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="emp">
  <select id="total" resultType="mybatis.vo.EmpVO">
    SELECT e.*, m.ename as manager, d.dname
	FROM emp e
	JOIN emp m
	ON m.empno = IFNULL(e.mgr,e.empno)
	JOIN dept d
	ON d.deptno = e.deptno
  </select>
  <select id="search" parameterType="Map" resultType="mybatis.vo.EmpVO">
    SELECT e.*, m.ename as manager, d.dname
	FROM emp e
	JOIN emp m
	ON m.empno = IFNULL(e.mgr,e.empno)
	JOIN dept d
	ON d.deptno = e.deptno
    <trim prefix="WHERE" >
    	<if test="searchType == 1">e.empno = #{searchValue}</if>
    	<if test="searchType == 2">e.ename LIKE CONCAT('%',#{searchValue},'%')</if>
   		<if test="searchType == 3">e.job LIKE CONCAT('%',#{searchValue},'%')</if>
   		<if test="searchType == 4">d.dname LIKE CONCAT('%',#{searchValue},'%')</if>
    </trim>
  </select>
</mapper>